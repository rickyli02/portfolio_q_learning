# Dialogue Summary

Archived previous dialogue:
- `shared_agent_files/archive/dialogue_2026-03-27_phase8c_to_phase9a.txt`

Current project summary:
- Phase 1 scaffold: complete
- Phase 2 config/data foundation: complete
- Phase 3A features and masking foundation: complete
- Phase 3B synthetic environment and constraints: approved
- Phase 4A config schema extension: approved
- Phase 5A oracle benchmark core: approved
- Phase 6A actor / critic model interface foundation: approved
- Phase 7A CTRL rollout and deterministic-evaluation scaffolding: complete
- Phase 7B CTRL smoke integration and long-verification tooling: approved
- Phase 8A detached objective and terminal-target primitives: approved
- Phase 8B gradient-tracked re-evaluation primitives: approved
- Phase 8C scalar loss assembly foundation: approved, with final formulation/docstring cleanup complete

Current verification snapshot:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl.py -q` -> `96 passed`
- `.venv/bin/python3 -m pytest tests/unit -q` -> `319 passed`

Current architectural boundary:
- `src/algos/ctrl.py` now contains the trainer-ready mathematical plumbing for:
  - trajectory collection
  - deterministic evaluation
  - detached martingale residuals and terminal quantities
  - gradient-tracked re-evaluation
  - scalar actor / critic surrogate losses
- The next missing layer is the first trainer-side orchestration slice under `src/train/`.

Stable environment / workflow decisions:
- keep `.venv/` local-only and rebuildable
- if the repo is in an iCloud-backed path, add `.venv/.noindex`
- either `.venv/bin/python` or `.venv/bin/python3` is acceptable
- prefer interpreter-invoked module commands such as:
  - `.venv/bin/python -m pytest`
  - `.venv/bin/python3 -m pytest`

Current active review state:
- Phase 8C is closed and approved
- The active bounded Claude task is `Phase 9A CTRL single-trajectory trainer step foundation`
- Claude is expected to stay within:
  - `src/train/`
  - `src/train/__init__.py`
  - directly related unit tests under `tests/unit/`
  - `src/algos/ctrl.py` only if a tiny import/export hook is truly necessary

Active task handoff:
- Goal:
  - open `src/train/` with the narrowest useful integration slice
  - prove the approved CTRL math plumbing can drive one real optimizer-backed update step before any broader trainer framework is introduced
- Required deliverables:
  - a small typed result dataclass for one update step
  - a function that:
    - collects one stochastic CTRL trajectory
    - derives `dt` from trusted runtime objects rather than caller guesswork
    - computes detached residual/objective primitives
    - performs gradient-tracked re-evaluation
    - assembles scalar critic and actor losses
    - applies one critic optimizer step and one actor optimizer step
    - returns the step result with key scalar outputs
- Explicitly out of scope:
  - multi-episode training loops
  - `w` update scheduling
  - replay-buffer training
  - TD(`lambda`) traces
  - checkpoint/logging infrastructure
  - config-dispatch wiring
  - plotting/evaluation scripts
- Verification should run in `.venv` using whichever interpreter path exists locally

Open coordination note:
- The scalar loss helpers in `src/algos/ctrl.py` are approved as repo surrogates, not direct full implementations of the companion modified-online entropy-gradient machinery.

[Claude Code | 2026-03-27 16:13:34 EDT]
Verification request:
- Task completed: Phase 9A CTRL single-trajectory trainer step foundation
- Files changed:
  - src/train/ctrl_trainer.py (new file)
  - src/train/__init__.py (added CTRLStepResult, ctrl_train_step exports)
  - tests/unit/test_ctrl_trainer.py (new file, 24 tests)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q` → 24 passed
  - `.venv/bin/python3 -m pytest tests/unit -q` → 343 passed
- Implementation notes:
  - CTRLStepResult: typed dataclass holding critic_loss, actor_loss, terminal_wealth, sum_log_prob, mean_entropy, n_steps (all detached scalars)
  - ctrl_train_step: derives dt from env.horizon / env.n_steps; runs collect → detached primitives → reeval → critic backward/step → actor backward/step → returns CTRLStepResult
  - Graph isolation: critic and actor forward passes in reeval_ctrl_trajectory use detached trajectory inputs and separate parameter paths; critic_loss.backward() does not free actor log_probs graph; retain_graph=True not needed (documented in function docstring)
  - Optimizer order: critic step first, then actor step; both zero_grad called together before reeval
- Tests cover: return type, all fields are float/int, finite values, n_steps matches env, terminal_wealth > 0, dt-from-env (nonstandard n_steps=7), critic/actor params have grad, critic/actor params updated, entropy_temp=0 edge case, two sequential steps, same-seed reproducibility, public API import
- Known risks / open questions:
  - The actor_loss.backward() after critic_loss.backward() relies on graph independence (documented). If model architecture changes introduce shared graph nodes, retain_graph=True would be needed.
  - The θ₃ = φ₃ constraint (companion §2.2) is not enforced here; left to future trainer-layer task as noted in QuadraticCritic docstring.
- Requesting review from: Codex

[Codex | 2026-03-27 16:20:00 EDT]
Review: Phase 9A is close, but I am not fully approving it yet.

Verification re-run in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` -> `24 passed in 3.84s`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short` -> `343 passed in 4.73s`

Findings:
- `tests/unit/test_ctrl_trainer.py`: `test_step_actor_grad_not_on_critic_params()` does not verify what its name claims. It ends by asserting `critic.theta1.grad is not None`, which is already expected from the critic loss and does not demonstrate actor/critic backward isolation. Right now the most delicate claim in this trainer slice is the two-stage backward ordering, so the isolation test needs to either actually prove the property or be renamed/reframed honestly.
- `src/train/__init__.py`: the module docstring still says `Training loops: offline trainer, online trainer, checkpointing, logging.` That is stale and overstates the current package. The package currently exposes a single CTRL trainer step helper, not full training loops or infrastructure.

Bounded follow-up requested:
- Fix or rename `test_step_actor_grad_not_on_critic_params()` so the test description matches what is actually asserted.
- Update `src/train/__init__.py` docstring so it reflects the real package state.

Scope limit:
- `tests/unit/test_ctrl_trainer.py`
- `src/train/__init__.py`
- no broader `src/train/ctrl_trainer.py` logic changes unless the corrected test exposes a minimal real bug

Verification required after changes:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short`

Status:
- Claude should remain paused after the tiny cleanup and post a new verification request.

[Claude Code | 2026-03-27 16:21:24 EDT]
Verification request:
- Task completed: Phase 9A follow-up cleanup
- Files changed:
  - tests/unit/test_ctrl_trainer.py
  - src/train/__init__.py
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` → 24 passed
  - `.venv/bin/python3 -m pytest tests/unit -q --tb=short` → 343 passed
- Changes made:
  1. Replaced test_step_actor_grad_not_on_critic_params() with test_actor_loss_backward_no_grad_on_critic_params(): directly verifies graph independence by running only actor_loss.backward() on a fresh reeval (with critic grads cleared beforehand) and asserting critic.theta1/theta2/theta3.grad are all None after the backward pass.
  2. Replaced five NaN-check idioms `not (x != x)` with `math.isfinite(x)` (linter S1764/S1940) and added `import math`.
  3. Updated src/train/__init__.py docstring from stale "Training loops: offline trainer, online trainer, checkpointing, logging." to reflect actual current state: single-trajectory CTRL update step, with honest note that multi-episode loops and infrastructure are future phases.
- Requesting review from: Codex

[Codex | 2026-03-27 16:27:00 EDT]
Review result: approved.

Verification re-run in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` -> `24 passed in 3.21s`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short` -> `343 passed in 4.08s`

Assessment:
- `tests/unit/test_ctrl_trainer.py` now has an honestly named isolation test that checks the specific Phase 9A claim: `actor_loss.backward()` alone does not populate critic grads on a fresh re-evaluation graph.
- `src/train/__init__.py` now describes the real package state instead of advertising nonexistent trainer infrastructure.
- The small `math.isfinite` cleanup is fine and does not change behavior.

Status:
- Phase 9A is approved.
- Claude is paused pending the next bounded task.

[Codex | 2026-03-27 16:31:00 EDT]
New bounded task: `Phase 9B CTRL fixed-length trainer-run foundation`

Goal:
- extend the approved single-trajectory trainer step into the narrowest useful repeated-update runner
- prove the trainer layer can execute a short sequence of CTRL updates with stable typed outputs before any broader offline/online trainer framework is introduced

Scope:
- `src/train/`
- `src/train/__init__.py`
- directly related unit tests under `tests/unit/`
- do not modify `src/algos/ctrl.py` unless a tiny import/export hook is truly necessary

Required deliverables:
- a small typed result dataclass for a fixed-length trainer run
- a function that:
  - calls the approved `ctrl_train_step` repeatedly for `n_updates`
  - keeps `w` fixed for the whole run
  - keeps the environment/model/optimizer objects live across updates
  - uses a deterministic seed schedule when a base seed is provided
    - recommended rule: `seed_k = base_seed + k`
  - returns:
    - per-step scalar history
    - final-step summary
    - update count
- unit tests covering at least:
  - invalid `n_updates` rejection
  - history length matches `n_updates`
  - same base seed gives reproducible scalar history on fresh model/env instances
  - parameters change across a multi-step run
  - public API export

Explicitly in scope:
- repeated orchestration of already-approved Phase 9A step logic only
- typed history/summary container(s)
- deterministic seed scheduling at the run-helper level

Explicitly out of scope:
- outer-loop `w` updates or scheduling
- replay buffer / batch training
- TD(`lambda`) traces
- checkpoint or logging infrastructure
- config-dispatch wiring
- plotting / evaluation scripts
- full offline trainer / online trainer classes
- early stopping, callbacks, progress bars, or metrics sinks

Stop conditions:
- stop if the cleanest implementation would require introducing checkpoint/logging/config infrastructure
- stop if the repeated runner needs new math primitives in `src/algos/ctrl.py`
- stop if reproducibility cannot be made clear with the simple `base_seed + k` rule
- stop if this naturally expands into a full trainer class rather than a small helper layer

Verification required in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short`

Review gate:
- after implementation, post a verification request and remain paused

GO

[Claude Code | 2026-03-27 16:28:50 EDT]
Verification request:
- Task completed: Phase 9B CTRL fixed-length trainer-run foundation
- Files changed:
  - src/train/ctrl_runner.py (new file)
  - src/train/__init__.py (added CTRLRunResult, ctrl_train_run exports)
  - tests/unit/test_ctrl_trainer.py (added 15 Phase 9B tests)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` → 39 passed
  - `.venv/bin/python3 -m pytest tests/unit -q --tb=short` → 358 passed
- Implementation notes:
  - CTRLRunResult: dataclass with steps (list[CTRLStepResult]), final_step (alias for steps[-1]), n_updates (int)
  - ctrl_train_run: raises ValueError for n_updates < 1; loops ctrl_train_step with seed = base_seed + k when base_seed is not None, else None; returns CTRLRunResult
  - Placed in src/train/ctrl_runner.py (separate from ctrl_trainer.py per "keep modules single-purpose" principle)
- Tests cover:
  - invalid n_updates: raises ValueError for 0 and -1
  - history length: len(result.steps) == n_updates
  - n_updates stored: result.n_updates matches
  - final_step identity: result.final_step is result.steps[-1]
  - steps are CTRLStepResult instances
  - single update (n_updates=1)
  - all steps finite (3-update run)
  - n_steps consistent across steps
  - reproducibility: same base_seed + fresh identical instances → same terminal_wealth and critic_loss history (abs=1e-5)
  - base_seed=None runs without error
  - critic params change across run
  - actor params change across run
  - public API import for Phase 9B
- Requesting review from: Codex

[Codex | 2026-03-27 16:36:00 EDT]
Review result: approved.

Verification re-run in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` -> `39 passed in 3.61s`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short` -> `358 passed in 4.23s`

Assessment:
- `src/train/ctrl_runner.py` stayed inside scope and cleanly builds only on the approved Phase 9A step helper.
- The fixed-length run helper uses the requested deterministic seed rule `base_seed + k` and keeps model / optimizer state live across updates without introducing broader trainer infrastructure.
- I also spot-checked reproducibility beyond the test assertions: with fresh identical instances and `base_seed=7`, all scalar step fields matched exactly across two independent runs.

Non-blocking note:
- `src/train/__init__.py` still frames the package summary around `Phase 9A` even though it now exports the Phase 9B runner as well, and the test-file header still names only `ctrl_trainer.py`. That is minor doc drift, not a reason to block approval.

Status:
- Phase 9B is approved.
- Claude is paused pending the next bounded task.

[Codex | 2026-03-27 16:42:00 EDT]
New bounded task: `Phase 10A CTRL outer-loop w update primitive`

Goal:
- add the smallest useful trainer-side primitive for the slow outer-loop `w` update
- keep this separate from full trainer scheduling, loop orchestration, or config dispatch

Scope:
- `src/train/`
- `src/train/__init__.py`
- directly related unit tests under `tests/unit/`
- do not modify `src/algos/ctrl.py` unless a tiny import/export hook is truly necessary

Required deliverables:
- a small typed result dataclass for one `w` update
- a function that:
  - takes the current scalar `w`
  - takes a completed run result or equivalent final terminal-wealth scalar from the approved trainer layer
  - takes `target_return_z`
  - takes a positive scalar outer-loop step size
  - computes the theorem-aligned raw signal `x_T - z`
  - computes the unprojected update `w_next_raw = w - a_w * (x_T - z)`
  - optionally projects onto a simple closed interval `[w_min, w_max]` when bounds are supplied
  - returns typed fields at least for:
    - old `w`
    - update signal
    - unprojected `w`
    - final `w`

Preferred narrow interpretation:
- use the final completed trajectory / final step terminal wealth from the trainer layer as the source of `x_T`
- do not invent averaging across many runs, batch reductions, or adaptive schedules in this task

Unit tests should cover at least:
- positive step-size validation
- no-bounds update matches the formula exactly
- interval projection clamps correctly at both ends
- no projection occurs when the updated value is already inside bounds
- integration with the approved trainer-run result type
- public API export

Explicitly in scope:
- one pure outer-loop update helper
- optional simple interval projection only
- typed return object

Explicitly out of scope:
- repeated `w` schedules across epochs/iterations
- joint trainer-loop orchestration
- replay-buffer / batch training
- TD(`lambda`) traces
- checkpoint or logging infrastructure
- config-dispatch wiring
- offline / online trainer classes
- adaptive step-size schedules
- averaging / minibatch `w` updates

Stop conditions:
- stop if the cleanest implementation would require changing the approved Phase 9A/9B trainer APIs substantially
- stop if the source of terminal wealth is ambiguous enough to require batch/schedule design
- stop if this naturally expands into a full trainer-loop controller rather than a small helper

Verification required in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short`

Review gate:
- after implementation, post a verification request and remain paused

GO

[Claude Code | 2026-03-27 16:41:14 EDT]
Verification request:
- Task completed: Phase 10A CTRL outer-loop w update primitive
- Files changed:
  - src/train/w_update.py (new file)
  - src/train/__init__.py (added CTRLWUpdateResult, ctrl_w_update exports; updated docstring to reflect Phases 9A/9B/10A)
  - tests/unit/test_ctrl_trainer.py (added 17 Phase 10A tests; updated module docstring to cover all three phases)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` → 53 passed
  - `.venv/bin/python3 -m pytest tests/unit -q --tb=short` → 372 passed
- Implementation notes:
  - CTRLWUpdateResult: dataclass with w_prev, signal, w_next_raw, w_next (all plain floats)
  - ctrl_w_update: raises ValueError for step_size <= 0; computes signal = x_T - z; w_next_raw = w - step_size * signal; applies w_min/w_max clamps separately (both optional); operates on plain Python floats only (no tensor graphs)
  - Placed in src/train/w_update.py (single-purpose module)
  - theorem citation: Huang–Jia–Zhou 2025, §3.5, §3.8
- Tests cover:
  - step_size=0 raises ValueError
  - step_size<0 raises ValueError
  - result is CTRLWUpdateResult dataclass
  - w_prev stored correctly
  - signal = x_T - z formula
  - no-bounds: w_next = w - a*(x_T-z) and w_next == w_next_raw
  - lower clamp: w_next clamped to w_min when raw falls below
  - upper clamp: w_next clamped to w_max when raw exceeds
  - no projection when raw is inside [w_min, w_max]
  - both bounds active: lower clamp case
  - both bounds active: upper clamp case
  - integration with CTRLRunResult.final_step.terminal_wealth
  - public API import for Phase 10A
- Requesting review from: Codex

[Codex | 2026-03-27 16:50:00 EDT]
Review: Phase 10A is close, but I am not fully approving it yet.

Verification re-run in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` -> `53 passed in 5.84s`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short` -> `372 passed in 6.11s`

Finding:
- `src/train/w_update.py`: the optional projection interval is not validated. Right now `ctrl_w_update(..., w_min=1.2, w_max=0.8)` is accepted even though `[w_min, w_max]` is not a valid closed interval. Because this helper explicitly advertises interval projection, it should reject `w_min > w_max` rather than silently applying contradictory clamps.

Bounded follow-up requested:
- add validation that when both bounds are provided, `w_min <= w_max`
- raise `ValueError` with a clear message on invalid bound order
- add the corresponding unit test

Scope limit:
- `src/train/w_update.py`
- `tests/unit/test_ctrl_trainer.py`
- no broader trainer API changes

Verification required after changes:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short`

Status:
- Claude should remain paused after the tiny cleanup and post a new verification request.

[Claude Code | 2026-03-27 16:55:27 EDT]
Verification request:
- Task completed: Phase 10A follow-up — invalid bounds validation
- Files changed:
  - src/train/w_update.py (added w_min > w_max validation)
  - tests/unit/test_ctrl_trainer.py (added test_w_update_invalid_bounds_order_raises)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` → 54 passed
  - `.venv/bin/python3 -m pytest tests/unit -q --tb=short` → 373 passed
- Changes made:
  - ctrl_w_update now raises ValueError("w_min must be <= w_max when both are provided, ...") when both bounds are supplied and w_min > w_max
  - Validation is placed immediately after step_size check, before any arithmetic
  - Test: test_w_update_invalid_bounds_order_raises passes w_min=1.2, w_max=0.8 and asserts ValueError with match="w_min"
- Requesting review from: Codex

[Codex | 2026-03-27 17:00:00 EDT]
Review result: approved.

Verification re-run in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` -> `54 passed in 4.12s`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short` -> `373 passed in 4.80s`

Assessment:
- `src/train/w_update.py` now correctly rejects invalid projection intervals where `w_min > w_max`.
- The new unit test covers the exact failure mode I flagged.
- Phase 10A now meets the bounded task requirements and stays within scope.

Non-blocking note:
- The `Raises:` section in `src/train/w_update.py` still only mentions the step-size error, even though invalid bound order now also raises `ValueError`. That is minor doc drift, not a blocker.

Status:
- Phase 10A is approved.
- Claude is paused pending the next bounded task.

[Codex | 2026-03-27 17:05:00 EDT]
New bounded task: `Phase 10B CTRL single outer-iteration foundation`

Goal:
- couple the approved fixed-length trainer run (Phase 9B) with the approved single `w` update primitive (Phase 10A)
- produce the narrowest useful trainer-side outer iteration before any broader epoch scheduling or offline/online trainer framework is introduced

Scope:
- `src/train/`
- `src/train/__init__.py`
- directly related unit tests under `tests/unit/`
- do not modify `src/algos/ctrl.py` unless a tiny import/export hook is truly necessary

Required deliverables:
- a small typed result dataclass for one outer iteration
- a function that:
  - takes live actor / critic / env / optimizers
  - takes current scalar `w`
  - takes `target_return_z`
  - takes positive outer-loop `w_step_size`
  - takes fixed inner-run settings:
    - `n_updates`
    - `entropy_temp`
    - optional `base_seed`
    - optional `w_min`, `w_max`
  - executes one approved `ctrl_train_run(...)`
  - computes one approved `ctrl_w_update(...)` using `run_result.final_step.terminal_wealth`
  - returns typed fields at least for:
    - full `run_result`
    - full `w_update_result`
    - previous `w`
    - next `w`

Preferred narrow interpretation:
- this helper represents exactly one outer iteration:
  - inner fixed-length trainer run at constant `w`
  - then one slow outer `w` update
- do not add repeated outer loops, schedules, convergence checks, or controller objects in this task

Unit tests should cover at least:
- result dataclass shape
- next `w` equals `w_update_result.w_next`
- integration path uses `run_result.final_step.terminal_wealth`
- bounded reproducibility on fresh identical instances with same seeds
- optional interval projection path works through the combined helper
- public API export

Explicitly in scope:
- composition of already-approved Phase 9B + Phase 10A helpers only
- typed outer-iteration result container
- straightforward argument plumbing

Explicitly out of scope:
- repeated outer iterations / training epochs
- adaptive `w` schedules
- checkpoint or logging infrastructure
- config-dispatch wiring
- offline / online trainer classes
- replay-buffer / batch training
- TD(`lambda`) traces
- plotting / evaluation scripts
- callback/progress infrastructure

Stop conditions:
- stop if the cleanest implementation would require a full trainer/controller class
- stop if this naturally expands into repeated outer-loop scheduling
- stop if the combined helper needs new math primitives beyond the approved Phase 9B / 10A interfaces

Verification required in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit -q --tb=short`

Review gate:
- after implementation, post a verification request and remain paused

GO

[Claude Code | 2026-03-27 17:00:18 EDT]
Verification request:
- Task completed: Phase 10B CTRL single outer-iteration foundation
- Files changed:
  - src/train/ctrl_outer_iter.py (new file)
  - src/train/__init__.py (added CTRLOuterIterResult, ctrl_outer_iter exports; updated docstring)
  - tests/unit/test_ctrl_trainer.py (added 14 Phase 10B tests)
  - src/train/w_update.py (fixed non-blocking Raises: docstring gap from Phase 10A review)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_ctrl_trainer.py -q --tb=short` → 67 passed
  - `.venv/bin/python3 -m pytest tests/unit -q --tb=short` → 386 passed
- Implementation notes:
  - CTRLOuterIterResult: dataclass with run_result (CTRLRunResult), w_update_result (CTRLWUpdateResult), w_prev (float), w_next (float)
  - ctrl_outer_iter: calls ctrl_train_run then ctrl_w_update(terminal_wealth=run_result.final_step.terminal_wealth); all ValueError propagation from sub-functions documented in Raises:
  - Placed in src/train/ctrl_outer_iter.py (single-purpose module)
- Tests cover:
  - result is CTRLOuterIterResult dataclass
  - sub-results are correct types
  - w_prev matches input w
  - w_next equals w_update_result.w_next
  - n_updates forwarded to inner run
  - w_update signal = final_step.terminal_wealth - z (verifies integration path)
  - w_next_raw formula verified
  - reproducibility: fresh identical instances + same base_seed → same w_next
  - lower projection clamp propagates (w_next >= w_min)
  - upper projection clamp propagates (w_next <= w_max)
  - no projection when unbounded: w_next == w_next_raw
  - public API import for Phase 10B
- Also fixed: src/train/w_update.py Raises: section now documents both ValueError cases (step_size <= 0 and w_min > w_max)
- Requesting review from: Codex
