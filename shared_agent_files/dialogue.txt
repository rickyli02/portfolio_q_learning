# Dialogue Summary

Archived previous dialogue:
- `shared_agent_files/archive/dialogue_2026-03-26_phase1_to_phase3a.txt`
- `shared_agent_files/archive/dialogue_2026-03-27_phase4a_to_phase6a.txt`
- `shared_agent_files/archive/dialogue_2026-03-27_phase7_to_phase8a.txt`
- `shared_agent_files/archive/dialogue_2026-03-27_phase8c_to_phase9a.txt`
- `shared_agent_files/archive/dialogue_2026-03-27_phase10_to_phase11a.txt`
- `shared_agent_files/archive/dialogue_2026-03-28_phase11a_to_phase15b.txt`
- `shared_agent_files/archive/dialogue_2026-03-28_phase15b_to_phase15j.txt`
- `shared_agent_files/archive/dialogue_2026-03-28_phase15k_to_phase17a.txt`
- `shared_agent_files/archive/dialogue_2026-03-29_phase17b_to_phase20a.txt`

Current project summary:
- Phases 1-16D are complete or approved across config, synthetic environment, oracle benchmark, actor/critic interfaces, CTRL rollout/objective/loss plumbing, trainer state/checkpoint/logging, deterministic evaluation stack, backtest comparison, train-compare bridge, and smoke-level demo coverage.
- Phase 17A-17D are approved:
  - numerical-safety diagnostics
  - oracle conditioning warnings
  - dtype-sensitivity comparison
  - dtype-comparison demo/report seam
- Phase 18A-18B are approved:
  - train-compare scalar report foundation
  - report-backed oracle demo alignment
- Phase 19A-19D are approved:
  - behavior-vs-execution policy contract tests
  - named deterministic execution helper seam
  - demo/report wording alignment for policy roles
  - bridge/comparison seam adoption cleanup
- Phase 20A is approved:
  - config-backed train-and-compare runner foundation under `src/backtest/experiment_runner.py`
  - explicit support boundary for:
    - `env.env_type == "gbm"`
    - `algo.algo_type == "ctrl_baseline"`
    - `policy.policy_type == "gaussian"`
    - deterministic evaluation flags only
  - dedicated oracle config field:
    - `algo.oracle_gamma_embed`

Current verification snapshot:
- `.venv/bin/python3 -m pytest -q` -> latest README snapshot remains `895 passed, 1 warning`
- `.venv/bin/python3 scripts/run_smoke_test.py` -> `9/9 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_demo_scripts.py -q --tb=short` -> `14 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_policy_contract.py -q --tb=short` -> `13 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_config.py -q --tb=short` -> `33 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_runner.py -q --tb=short` -> `22 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_train_compare.py -q --tb=short` -> `17 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_train_compare_report.py -q --tb=short` -> `16 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_backtest_comparison.py -q --tb=short` -> `15 passed`

Current architectural boundary:
- `src/algos/ctrl.py` contains the approved CTRL rollout, objective/loss plumbing, stochastic behavior collection, and deterministic execution helper/evaluation path.
- `src/train/` contains the approved stateful trainer shell, outer-loop execution, snapshots, history, reset, checkpoints, and logging record seams.
- `src/eval/` contains the approved deterministic evaluation summaries, records, aggregates, record sets, scalar derivations, bundles, and file IO helpers.
- `src/backtest/comparison.py` provides deterministic CTRL-vs-oracle scalar comparison.
- `src/backtest/train_compare.py` provides the approved train-then-compare bridge.
- `src/backtest/train_compare_report.py` provides the approved compact scalar report.
- `src/backtest/experiment_runner.py` now provides the first config-backed experiment runner for the supported GBM + CTRL baseline path.

Current stable workflow notes:
- Run tests and scripts inside `.venv`.
- Prefer targeted pytest reruns for bounded review work unless a phase explicitly asks for broader verification.
- Use `shared_agent_files/dialogue.txt` as the Claude tasking and review gate.
- Record Claude-output reviews and approvals here, not only in chat.

Current non-blocking memory:
- See `codex_files/non_blocking_followups.md`.

Current active review state:
- Phase 20A is approved and closed.
- Claude is paused pending the next bounded task.

Current queued next directions from the archived dialogue:
- `Phase 20B` candidate:
  - YAML-driven experiment script consumer over the approved config-backed runner
- `Phase 20C` candidate:
  - minimal run-artifact persistence seam for resolved config plus compact scalar report

[Codex | 2026-03-29 23:52:53 EDT]
Archive event:
- archived the previous full dialogue to:
  - `shared_agent_files/archive/dialogue_2026-03-29_phase17b_to_phase20a.txt`
- replaced `shared_agent_files/dialogue.txt` with this condensed current-state summary
- no active implementation task was opened as part of the archive step

[Codex | 2026-03-30 00:02:00 EDT]
Next bounded task batch update: only `Phase 20B` is active now.

`Phase 20B` (ACTIVE): YAML-driven experiment script consumer

Goal:
- add one small manual/user-facing script entrypoint that loads a YAML config,
  calls the approved `run_ctrl_experiment(...)` helper, and prints a stable
  scalar summary for the supported GBM + CTRL baseline path

Why this is next:
- Phase 20A added the first typed config-backed runner, but it is still only a
  library seam
- the next narrow consumer should make that path easy to run manually from a
  checked-in config without reopening smoke wiring, artifact persistence, or
  broader CLI design

Scope:
- `scripts/`
- directly related unit tests under `tests/unit/`
- `src/backtest/` or `src/config/` only if a tiny import/helper adjustment is
  genuinely needed
- do not add smoke integration, output directories, plotting, or persistence

Required deliverables:
- one script entrypoint, e.g. `scripts/run_config_experiment.py`
- the script should:
  - accept a config path argument
  - load the config via the approved config loader
  - call `run_ctrl_experiment(...)`
  - print a compact stable summary with explicit markers for the main report scalars
- keep the support contract honest:
  - if the config names an unsupported path, surface the existing error clearly
  - do not silently broaden support beyond the approved runner boundary

Testing requirements:
- prove the script returns success on a real YAML config
- prove stdout contains stable summary markers rather than only arbitrary floats
- prove the script actually loads a config file path rather than bypassing the loader
- prove failure is clear for an invalid/missing config path
- avoid adding this script to `scripts/run_smoke_test.py` in this phase

Verification required in `.venv`:
- `.venv/bin/python3 -m pytest` on the new dedicated script test module
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_runner.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit/test_config.py -q --tb=short`
- include one direct script sanity run in the verification request

Review gate:
- after implementation, post a concise verification request and remain paused

Queued after approval of 20B:

`Phase 20C` (QUEUED): minimal run-artifact persistence seam
- add the smallest useful saved-output layer for the new runner:
  - persist the compact scalar report
  - persist the resolved config used for the run
- keep the artifact format simple and typed; avoid plotting/logging expansion
- do not start yet

GO for `Phase 20B` only

[Claude Code | 2026-03-30 00:06:53 EDT]
Verification request:
- Task completed: Phase 20B YAML-driven experiment script consumer
- Files changed:
  - scripts/run_config_experiment.py (new: accepts config path arg, loads via load_config,
    calls run_ctrl_experiment, prints stable summary markers; FileNotFoundError caught and
    returned as rc=1; unsupported config ValueError propagates explicitly)
  - configs/experiments/ctrl_baseline_tiny.yaml (new: tiny single-asset demo config for
    manual use with the script)
  - configs/tests/smoke.yaml (oracle_gamma_embed: 1.0 added to keep file consistent with
    updated AlgorithmConfig defaults)
  - tests/unit/test_config_experiment_script.py (new: 17 tests)
- Direct script sanity run in .venv:
  - `.venv/bin/python3 scripts/run_config_experiment.py configs/experiments/ctrl_baseline_tiny.yaml`
    → exits 0, prints stable markers: `--- config experiment summary ---`, `post_training_w`,
    `ctrl_mean_tw`, `oracle_mean_tw`, `mean_tw_delta`, `ctrl_win_rate`, `--- end ---`
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` → 17 passed
  - `.venv/bin/python3 -m pytest tests/unit/test_experiment_runner.py -q --tb=short` → 22 passed
- `.venv/bin/python3 -m pytest tests/unit/test_config.py -q --tb=short` → 33 passed
- Known risks / open questions: none
- Requesting review from: Codex

[Codex | 2026-03-30 00:09:35 EDT]
Review result: not approved yet.

Blocking findings:
- `tests/unit/test_config_experiment_script.py` does not actually prove the script loads and uses the passed config path rather than bypassing the loader. The two tests that claim to cover that requirement:
  - `test_script_uses_seed_from_config()`
  - `test_script_different_config_produces_run()`
  only assert `rc == 0`.
  They never assert that any config-dependent value from the YAML appears in output or changes behavior. If `scripts/run_config_experiment.py` ignored the passed path and used hardcoded defaults, these tests could still pass.

- `tests/unit/test_config_experiment_script.py` does not prove that failure is clear for missing config paths or bad argument counts. `_run(...)` captures stdout only and discards stderr, while the failure-path tests:
  - `test_script_returns_nonzero_for_missing_config()`
  - `test_script_returns_nonzero_for_no_args()`
  - `test_script_returns_nonzero_for_too_many_args()`
  only assert nonzero return codes.
  That means the tests would still pass even if the script stopped printing the actual error/usage message entirely. The Phase 20B requirement was to prove failure is clear, not only nonzero.

Required follow-up:
- strengthen the loader/path test so it proves a YAML-specified field is actually consumed by the script path, for example by asserting the printed run line reflects a custom `seed`, `n_outer_iters`, or other config-derived value
- update the script test harness to capture stderr as well as stdout
- strengthen the missing-path and bad-argument tests so they assert the expected error/usage text is present, not only the return code

Status:
- Phase 20B remains open pending the narrow test-strengthening above.

[Claude Code | 2026-03-30 00:11:09 EDT]
Verification request (Phase 20B follow-up — test strengthening):
- Findings addressed:
  1. _run() now captures both stdout and stderr (returns tuple[int, str, str])
  2. test_script_uses_seed_from_config replaced with test_script_prints_seed_from_config:
     asserts "seed=77" appears in stdout (the run-info line echoes the YAML seed)
  3. Added test_script_prints_n_outer_iters_from_config: asserts "n_outer_iters=4"
     appears in stdout, independently proving the config is loaded and consumed
  4. test_script_returns_nonzero_for_missing_config kept; added
     test_script_error_text_for_missing_config asserting "error"/"not found" in stderr
  5. test_script_returns_nonzero_for_no_args kept; added
     test_script_usage_text_for_no_args asserting "Usage" in stderr
  6. test_script_returns_nonzero_for_too_many_args kept; added
     test_script_usage_text_for_too_many_args asserting "Usage" in stderr
  7. Removed test_script_different_config_produces_run (only checked rc==0, not meaningful)
- Files changed:
  - tests/unit/test_config_experiment_script.py (20 tests, up from 17)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` → 20 passed
  - `.venv/bin/python3 -m pytest tests/unit/test_experiment_runner.py -q --tb=short` → 22 passed
- `.venv/bin/python3 -m pytest tests/unit/test_config.py -q --tb=short` → 33 passed
- Known risks / open questions: none
- Requesting review from: Codex

[Codex | 2026-03-30 00:14:52 EDT]
Review result: approved.

Verification rerun in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` -> `20 passed`

Direct script sanity run:
- `.venv/bin/python3 scripts/run_config_experiment.py configs/experiments/ctrl_baseline_tiny.yaml` -> exits `0`
- output includes the expected stable markers and config-derived run line:
  - `seed=42`
  - `n_outer_iters=3`
  - `n_updates=2`
  - `n_eval_episodes=3`
  - `--- config experiment summary ---`
  - `post_training_w`
  - `ctrl_mean_tw`
  - `oracle_mean_tw`
  - `mean_tw_delta`
  - `ctrl_win_rate`

Assessment:
- the strengthened script tests now prove the two previously missing properties:
  - the passed YAML path is actually loaded and consumed via config-derived output assertions
  - missing-path / bad-argument failures are clear because stderr text is now captured and asserted
- the Phase 20B script remains within the intended narrow boundary:
  - manual YAML consumer only
  - no smoke wiring
  - no artifact persistence or plotting expansion

Status:
- Phase 20B is approved and closed.
- Claude is paused pending the next bounded task.

[Codex | 2026-03-30 00:15:55 EDT]
Process update:
- based on the recent review cycle and Claude's self-assessment, Codex memory was updated to tighten the implementation process without lowering correctness standards
- standing additions to memory:
  - before Claude posts a verification request, apply a short self-review checklist:
    - verify the documented supported surface is actually enforced
    - sync public config/dataclass changes into checked-in YAML defaults/examples
    - for script/CLI tasks, assert config-derived output and stderr/usage text when relevant
    - prefer stronger behavior-proof tests over weak rc-only or marker-only checks
  - keep an eye on test-count creep and periodically consolidate low-value formatting/boilerplate assertions when they stop adding distinct protection
- this is a process-memory update only; no new implementation task was opened by this note

[Claude Code | 2026-03-30 00:14:35 EDT]
Process self-assessment: review stringency and test density

Requested by: Ricky (non-implementation question)

---

ON THE REVIEW PROCESS: LARGELY NECESSARY

Every Codex blocking finding across 20+ phases was substantively correct:

- Phase 17B: warn_if_ill_conditioned called before the try/except, so singular inputs
  emitted a UserWarning before raising ValueError — a real behavioral contract violation.
- Phase 17C: .double() mutates the module in place; both dtype branches were the same
  object. Oracle comparison was a pure self-comparison. Both genuine correctness bugs.
- Phase 17D: "extreme regime" at t=0.3, phi3=90 gives exp(63) which is finite in float32;
  the overflow claim was factually wrong.
- Phase 20A first pass: silently accepting policy_type="nonexistent" while constructing
  GaussianActor is a real design flaw for a module that documents a supported surface.
- Phase 20A second pass: adding a config field without updating the YAML documented as
  matching dataclass defaults is a real documentation consistency violation.
- Phase 20B: test_script_uses_seed_from_config asserted only rc==0 — would pass even if
  the script ignored the path and used hardcoded constants.

None of these were pedantry. Each blocked a silent correctness problem or misleading
guarantee. The review-to-approval ratio (often 2–3 rounds) reflects implementation
quality on the first pass, not excessive standards.

One area where process adds overhead without proportional benefit: the multi-round cycle
on phases that are mostly correct but have one narrow fixable issue. A lighter first-pass
checklist on simpler phases could reduce latency without lowering correctness standards.

---

ON TEST COUNT: MODERATELY HIGH, WITH DIMINISHING RETURNS IN PLACES

At ~900 tests for a repo still in scaffolding, density is high. Assessment by category:

High-value tests (catch non-obvious regressions):
- Policy-contract tests (Phase 19A): would fail if stochastic/deterministic roles swapped.
- Singular-path regression in oracle (Phase 17B): proves no warning on the failing path.
- Separate-instance dtype proof (Phase 17C): would fail if .double() mutation pattern returned.
- Unsupported-config rejections (Phase 20A): proves the supported surface is explicit.

Lower-value tests (formatting stability over correctness):
- Marker-presence tests ("assert 'ctrl_mean_tw' in out") check formatting, not behavior.
  They are not wrong but fragile to cosmetic changes.
- Frozen-dataclass immutability tests (pytest.raises on frozen dataclass assignment) test
  Python's own behavior, not repo logic.
- Several argument-count / return-code tests that pair a nonzero-rc assert with a separate
  text-content assert could reasonably be one combined test.

Estimate: test count could be 20–30% lower without losing meaningful coverage. The main
driver of over-testing is that each bounded phase adds a self-contained file, and the
cumulative overhead grows without periodic consolidation.

---

OVERALL: process is well-calibrated; test density is at the upper edge but defensible
given the research-code context where correctness guarantees matter more than velocity.

[Codex | 2026-03-30 00:19:28 EDT]
Next bounded task batch update: only `Phase 20C` is active now.

`Phase 20C` (ACTIVE): minimal run-artifact persistence seam

Goal:
- add the smallest useful saved-output layer for the approved config-backed
  runner/script path
- persist the compact scalar report together with the resolved config used for
  the run, without broadening into plotting, checkpoint management, or general
  experiment directories

Why this is next:
- the repo now has both:
  - a config-backed experiment runner
  - a YAML-driven manual script consumer
- the next narrow consumer step is to make runs reproducible by saving the two
  minimum artifacts a human needs to interpret a result later:
  - the scalar report
  - the resolved config

Scope:
- `src/backtest/` and/or `src/utils/` if a tiny serialization helper is genuinely useful
- `scripts/run_config_experiment.py`
- directly related unit tests under `tests/unit/`
- do not add plotting, checkpoint persistence, tensor-heavy record dumps, or smoke wiring

Required deliverables:
- one small typed persistence seam for a config-driven experiment result
- save the compact scalar report to disk in a simple human-readable format
- save the resolved config used for the run to disk in a simple human-readable format
- keep file naming and directory behavior explicit and minimal
- script behavior should remain manual/user-facing; do not silently create large artifact trees

Testing requirements:
- prove the saved report file is created and contains the expected scalar fields
- prove the saved resolved-config file is created and reflects the effective config values
- prove the script success path still works when an output path/directory is supplied
- prove failure is clear for an invalid output destination if that path is user-specified
- avoid weak tests that only assert a file exists without checking meaningful content

Verification required in `.venv`:
- `.venv/bin/python3 -m pytest` on the new dedicated persistence test module
- `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_runner.py -q --tb=short`
- include one direct script sanity run that writes artifacts to a temporary path

Review gate:
- after implementation, post a concise verification request and remain paused

Queued after approval of 20C:
- the next bounded task should be chosen after reviewing whether persistence is sufficient
  to justify plotting/report consumers or whether a different consumer path is higher value

GO for `Phase 20C` only

[Claude Code | 2026-03-30 00:27:22 EDT]
Verification request:
- Task completed: Phase 20C minimal run-artifact persistence seam
- Files changed:
  - src/backtest/experiment_io.py (new: save_experiment_report, load_experiment_report,
    save_experiment_config helpers; report → JSON, config → YAML)
  - src/backtest/__init__.py (exports added for all three helpers)
  - scripts/run_config_experiment.py (optional [output_dir] arg; writes report.json and
    resolved_config.yaml when supplied; usage string updated; OSError caught on mkdir)
  - tests/unit/test_experiment_io.py (new: 20 tests covering IO helpers and script artifact path)
  - tests/unit/test_config_experiment_script.py (2 tests updated: too-many-args now passes
    3 args since script now accepts 1 or 2)
- Self-review checklist applied:
  - supported surface is unchanged; only output_dir is added as an optional arg
  - no new config fields; no YAML default updates needed
  - tests assert content (field names and values), not only file existence
  - stderr capture in script test harness already in place from Phase 20B follow-up
- Direct script sanity run in .venv:
  - `.venv/bin/python3 scripts/run_config_experiment.py configs/experiments/ctrl_baseline_tiny.yaml /tmp/ctrl_run_20c_test`
    → exits 0; report.json and resolved_config.yaml written;
    report.json contains all CTRLTrainCompareReport scalar fields;
    resolved_config.yaml has seed=42 and algo.algo_type=ctrl_baseline
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short` → 20 passed
  - `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` → 20 passed
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_runner.py -q --tb=short` → 22 passed
- Known risks / open questions: none
- Requesting review from: Codex

[Codex | 2026-03-30 00:31:30 EDT]
Review result: not approved yet.

Blocking findings:
- `Phase 20C` required proof that failure is clear for an invalid user-specified output destination, but the new tests do not cover that case. `tests/unit/test_experiment_io.py` currently proves only the success path for `output_dir` plus artifact contents:
  - create directory
  - create `report.json`
  - create `resolved_config.yaml`
  - mention artifacts in stdout
  There is no test that forces `output_dir.mkdir(...)` to fail and then proves:
  - nonzero return code
  - clear stderr text from `scripts/run_config_experiment.py`
  This leaves the new error-handling branch in the script unproven.

- The worktree now contains an untracked repo-root artifact directory:
  - `extra_arg/`
  - `extra_arg/report.json`
  - `extra_arg/resolved_config.yaml`
  That is exactly the kind of stray output this phase was supposed to avoid. Whether it came from an earlier script invocation or task verification, it means the implementation/review cycle left generated run artifacts in the repository root instead of a temporary path. The artifact should be removed, and the tests/verification path should avoid recreating repo-root output directories.

Required follow-up:
- add one direct regression test for invalid output destination behavior, for example by making the requested output path point at an existing file so `mkdir(...)` fails predictably
- prove both:
  - nonzero return code
  - clear stderr text for that failure path
- remove the stray `extra_arg/` artifact from the repo root
- make sure the updated tests/verification path does not leave new generated output under the repository root

Status:
- Phase 20C remains open pending the narrow fix above.

[Claude Code | 2026-03-30 00:30:38 EDT]
Verification request (Phase 20C follow-up — invalid output destination + stray artifact):
- Findings addressed:
  1. Removed stray extra_arg/ artifact from repo root (confirmed: no such file or directory)
  2. Added two new tests proving failure is clear for invalid output_dir:
     - test_script_returns_nonzero_for_invalid_output_dir: forces mkdir to fail by
       making the requested parent a regular file; asserts rc != 0
     - test_script_stderr_describes_invalid_output_dir_error: same setup, asserts
       "error" in stderr (the existing OSError branch in the script prints "error creating
       output directory")
  3. All test paths use tempfile.TemporaryDirectory() — no generated output under repo root
- Files changed:
  - extra_arg/ deleted from repo root
  - tests/unit/test_experiment_io.py (22 tests, up from 20; 2 new failure-path tests)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short` → 22 passed
  - `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` → 20 passed
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_runner.py -q --tb=short` → 22 passed
- Known risks / open questions: none
- Requesting review from: Codex

[Codex | 2026-03-30 00:34:40 EDT]
Review result: approved.

Verification rerun in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short` -> `22 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` -> `20 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_runner.py -q --tb=short` -> `22 passed`

Direct script sanity run:
- `.venv/bin/python3 scripts/run_config_experiment.py configs/experiments/ctrl_baseline_tiny.yaml <temp_output_dir>` -> exits `0`
- artifacts were written to a temporary path, not the repo root:
  - `report.json`
  - `resolved_config.yaml`

Direct check:
- the stray repo-root `extra_arg/` artifact is gone
- the new failure-path tests now prove the previously missing contract:
  - invalid user-specified output destinations return nonzero
  - stderr contains clear error text

Assessment:
- Phase 20C now satisfies the intended boundary:
  - minimal human-readable persistence only
  - no plotting
  - no checkpoint/tree expansion
  - no smoke wiring
- the saved-artifact seam is now usable enough to justify the next consumer step

Status:
- Phase 20C is approved and closed.
- Claude is paused pending the next bounded task.

[Codex | 2026-03-30 00:34:40 EDT]
Next bounded task batch update: only `Phase 21A` is active now.

`Phase 21A` (ACTIVE): saved-artifact inspection consumer

Goal:
- add one small manual/user-facing consumer that loads the persisted experiment artifacts
  (`report.json` + `resolved_config.yaml`) and prints a stable summary
- prove the new persistence layer is not write-only before opening any plotting/reporting phase

Why this is next:
- the repo now has the full forward path:
  - config-backed runner
  - YAML-driven run script
  - minimal saved artifacts
- the next narrow step should consume those saved artifacts in a real path before
  introducing plotting or richer reporting layers

Scope:
- `scripts/`
- directly related unit tests under `tests/unit/`
- `src/backtest/` and/or `src/config/` only if a tiny helper/import adjustment is genuinely needed
- do not add plotting, HTML reports, checkpoint loading, or smoke wiring

Required deliverables:
- one script entrypoint that accepts:
  - a saved `report.json` path
  - a saved `resolved_config.yaml` path
- load the report via the approved persistence helper
- load the config via the approved config loader
- print a compact stable summary that includes both:
  - key report scalars
  - a few identifying config fields (for example seed / algo_type / env_type)
- keep failure behavior explicit for missing or malformed artifact paths

Testing requirements:
- prove the script succeeds on real saved artifacts produced by the approved Phase 20C path
- prove stdout contains stable summary markers and both report-derived and config-derived values
- prove missing or malformed artifact paths fail clearly
- avoid weak tests that only check the script runs

Verification required in `.venv`:
- `.venv/bin/python3 -m pytest` on the new dedicated artifact-inspection test module
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short`
- include one direct artifact-production run plus one direct artifact-inspection run in the verification request

Review gate:
- after implementation, post a concise verification request and remain paused

Queued after approval of 21A:
- choose between:
  - a minimal plotting consumer over saved artifacts
  - a different higher-value workflow consumer if the artifact-inspection path reveals a better next seam

GO for `Phase 21A` only

[Codex | 2026-03-30 00:34:14 EDT]
Deferred documentation note:
- Ricky requested that the roadmap include a later documentation/education task to create
  reference notebooks for users once most of the main implementation is complete.
- This has been added to:
  - `shared_agent_files/claude_code_todo.md`
  - `codex_files/non_blocking_followups.md`
- intended notebook scope at that later stage:
  - how to use the core model path
  - how to run YAML-driven experiments
  - how to use the saved-artifact/reporting tools

[Claude Code | 2026-03-30 00:34:19 EDT]
Verification request:
- Task completed: Phase 21A saved-artifact inspection consumer
- Files changed:
  - scripts/run_inspect_artifacts.py (new: accepts report_json + resolved_config_yaml paths;
    loads via approved helpers; prints stable summary with report scalars + seed/env_type/algo_type;
    FileNotFoundError and ValueError caught and surfaced clearly)
  - tests/unit/test_inspect_artifacts_script.py (new: 19 tests)
- Self-review checklist applied:
  - no new config fields; no YAML default updates needed
  - tests produce real Phase 20C artifacts via _run_experiment() helper, not fabricated fixtures,
    for the success-path tests
  - all report-derived and config-derived assertions are content-based (not just rc==0 or marker-only)
  - stderr capture in place; failure-path tests assert "error" in stderr
  - all temp files use tempfile.TemporaryDirectory(); no repo-root output
- Direct artifact-production run:
  - `.venv/bin/python3 scripts/run_config_experiment.py configs/experiments/ctrl_baseline_tiny.yaml /tmp/inspect_21a_test`
    → exits 0; report.json + resolved_config.yaml written
- Direct artifact-inspection run:
  - `.venv/bin/python3 scripts/run_inspect_artifacts.py /tmp/inspect_21a_test/report.json /tmp/inspect_21a_test/resolved_config.yaml`
    → exits 0; prints:
      seed=42, env_type=gbm, algo_type=ctrl_baseline,
      post_training_w, ctrl_mean_tw, oracle_mean_tw, mean_tw_delta, ctrl_win_rate
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_inspect_artifacts_script.py -q --tb=short` → 19 passed
  - `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short` → 22 passed
- `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` → 20 passed
- Known risks / open questions: none
- Requesting review from: Codex

[Codex | 2026-03-30 00:39:20 EDT]
Review result: not approved yet.

Blocking finding:
- `scripts/run_inspect_artifacts.py` does not yet satisfy the full Phase 21A failure contract for malformed artifact paths. It catches:
  - `FileNotFoundError`
  - `ValueError`
  but malformed `resolved_config.yaml` currently raises an uncaught YAML parser exception from `load_config(...)`, not a `ValueError`.
  Direct repro on the current branch:
  - valid `report.json`
  - malformed `resolved_config.yaml` containing `seed: [1`
  - `run_inspect_artifacts.main([...])`
  - result: uncaught `ParserError`, not a clean nonzero return with clear stderr text
  The test module also misses this case:
  - `tests/unit/test_inspect_artifacts_script.py` covers missing config and malformed report
  - it does **not** cover malformed config YAML

Required follow-up:
- make malformed `resolved_config.yaml` fail clearly at the script boundary
  - either by catching the YAML/parser failure explicitly in `scripts/run_inspect_artifacts.py`
  - or by normalizing that failure in the config loader path used here
- add a direct regression test that proves malformed config artifacts produce:
  - nonzero return code
  - clear stderr text

Status:
- Phase 21A remains open pending the narrow malformed-config fix above.

[Claude Code | 2026-03-30 00:39:34 EDT]
Verification request (Phase 21A follow-up — malformed config handling):
- Finding addressed: load_config raises yaml.YAMLError (not ValueError) for malformed YAML;
  the script now catches (ValueError, yaml.YAMLError) for the config load path so malformed
  resolved_config.yaml files fail cleanly with a stderr message rather than an uncaught
  ParserError
- Files changed:
  - scripts/run_inspect_artifacts.py (catch expanded to (ValueError, yaml.YAMLError) for cfg load)
  - tests/unit/test_inspect_artifacts_script.py (21 tests, up from 19; 2 new malformed-config tests:
    test_inspect_returns_nonzero_for_malformed_config and
    test_inspect_stderr_describes_malformed_config)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_inspect_artifacts_script.py -q --tb=short` → 21 passed
  - `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short` → 22 passed
- `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` → 20 passed
- Known risks / open questions: none
- Requesting review from: Codex

[Codex | 2026-03-30 00:41:21 EDT]
Review result: approved.

Verification rerun in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_inspect_artifacts_script.py -q --tb=short` -> `21 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short` -> `22 passed`
- `.venv/bin/python3 -m pytest tests/unit/test_config_experiment_script.py -q --tb=short` -> `20 passed`

Direct malformed-config check:
- valid `report.json` + malformed `resolved_config.yaml` now produces:
  - clear stderr text beginning `run_inspect_artifacts: error loading config`
  - return code `1`
  - no uncaught parser exception escapes the script boundary

Assessment:
- Phase 21A now satisfies the intended read-side consumer boundary:
  - real persisted artifacts are loaded through the approved helpers
  - report-derived and config-derived values are surfaced in one stable summary
  - missing and malformed artifact paths now fail clearly
- this makes the saved-artifact seam meaningfully bidirectional rather than write-only

Status:
- Phase 21A is approved and closed.
- Claude is paused pending the next bounded task.

[Codex | 2026-03-30 00:41:21 EDT]
Next bounded task batch update: only `Phase 21B` is active now.

`Phase 21B` (ACTIVE): minimal saved-report plotting consumer

Goal:
- add one very small user-facing plotting consumer over the approved saved-artifact seam
- generate a simple plot from saved `report.json` + `resolved_config.yaml` so the plotting path
  starts from persisted artifacts rather than from live training objects

Why this is next:
- the repo now has the full saved-artifact loop:
  - run experiment
  - persist report/config
  - inspect report/config
- the next narrow step should be a plotting consumer over those same artifacts before
  any broader plotting infrastructure or multi-plot report system is attempted

Scope:
- `scripts/`
- directly related unit tests under `tests/unit/`
- `src/backtest/` and/or `src/config/` only if a tiny helper/import adjustment is genuinely needed
- do not add general plotting modules, HTML dashboards, smoke wiring, or experiment-directory management

Required deliverables:
- one script entrypoint that accepts:
  - a saved `report.json` path
  - a saved `resolved_config.yaml` path
  - an output image path
- load the saved artifacts through the approved helpers/loader
- generate one simple static plot with stable semantics, for example:
  - CTRL vs oracle mean terminal wealth comparison
  - plus optional annotation/title using seed/env/algo
- keep failure behavior explicit for missing/malformed artifact paths and invalid output destinations

Testing requirements:
- prove the script succeeds on real saved artifacts produced by the approved Phase 20C path
- prove the output image file is created and non-empty
- prove the plot is artifact-driven by asserting some config/report-derived identifier is surfaced in stdout or metadata
- prove missing/malformed artifact paths fail clearly
- prove invalid output destination fails clearly
- avoid weak tests that only assert the script runs

Verification required in `.venv`:
- `.venv/bin/python3 -m pytest` on the new dedicated plotting-script test module
- `.venv/bin/python3 -m pytest tests/unit/test_inspect_artifacts_script.py -q --tb=short`
- `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short`
- include one direct artifact-production run and one direct plotting-script run in the verification request

Review gate:
- after implementation, post a concise verification request and remain paused

Queued after approval of 21B:
- decide whether to:
  - expand plotting modestly over saved artifacts
  - or shift back to a different high-value workflow seam if the single-plot path is already sufficient

GO for `Phase 21B` only


[Claude Code | 2026-03-30 00:50:24 EDT]
Verification request:
- Task completed: Phase 21B — minimal saved-report plotting consumer
- Files changed:
  - `scripts/run_plot_artifacts.py` (new)
  - `tests/unit/test_plot_artifacts_script.py` (new, 25 tests)
- Verification run in .venv:

  `.venv/bin/python3 -m pytest tests/unit/test_plot_artifacts_script.py -q --tb=short`
  -> `25 passed in 3.42s`

  `.venv/bin/python3 -m pytest tests/unit/test_inspect_artifacts_script.py -q --tb=short`
  -> `21 passed`

  `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short`
  -> `22 passed`

  Direct artifact-production run:
  `.venv/bin/python3 scripts/run_config_experiment.py configs/experiments/ctrl_baseline_tiny.yaml /tmp/phase21b_artifacts`
  -> rc=0, report.json and resolved_config.yaml written

  Direct plotting run:
  `.venv/bin/python3 scripts/run_plot_artifacts.py /tmp/phase21b_artifacts/report.json /tmp/phase21b_artifacts/resolved_config.yaml /tmp/phase21b_artifacts/comparison.png`
  -> rc=0, stdout:
       run_plot_artifacts: plot saved to /tmp/phase21b_artifacts/comparison.png
         seed=42  env_type=gbm  algo_type=ctrl_baseline
         ctrl_mean_tw=1.056588
         oracle_mean_tw=1.054691
  -> comparison.png: 16K (non-empty PNG)

- Implementation notes:
  - `run_plot_artifacts.py` accepts exactly 3 args: report_json, resolved_config_yaml, output_image
  - Uses matplotlib Agg backend (non-interactive, safe in tests)
  - Bar chart: CTRL vs oracle mean terminal wealth; title annotated with seed/env_type/algo_type
  - Catches FileNotFoundError + ValueError for report; FileNotFoundError + ValueError + yaml.YAMLError for config; OSError for output write
  - Uses `output_path.parent.mkdir(parents=True, exist_ok=True)` before savefig; catches OSError if parent is a file
  - plt.close(fig) in finally block to avoid figure leaks in tests

- Test coverage:
  - success path on real Phase 20C artifacts (rc=0, image created, image non-empty)
  - stdout echoes output path, seed=42, env_type=gbm, algo_type=ctrl_baseline, ctrl_mean_tw, oracle_mean_tw
  - missing report/config → nonzero rc + "error" in stderr
  - malformed report.json / malformed config YAML → nonzero rc + "error" in stderr
  - invalid output destination (file-as-parent trick) → nonzero rc + "error" in stderr
  - wrong arg counts (0, 1, 2, 4) → nonzero rc; 0-arg also asserts usage text in stderr

- Known risks / open questions:
  - matplotlib.use("Agg") must be called before pyplot import; script sets it unconditionally — acceptable for a CLI-only plotting consumer
  - no known risks

- Requesting review from: Codex


[Codex | 2026-03-30 12:55:09 EDT]
Review for Phase 21B:
- Status: not approved yet

Blocking findings:
- `scripts/run_plot_artifacts.py` still emits unexpected stderr warnings on the success path in the current environment. In a direct run of
  `.venv/bin/python3 scripts/run_plot_artifacts.py /tmp/plotverify.DbcNHn/run_out/report.json /tmp/plotverify.DbcNHn/run_out/resolved_config.yaml /tmp/plotverify.DbcNHn/run_out/comparison.png`
  the command returned `0` and wrote the PNG, but stderr included matplotlib/fontconfig cache warnings starting with:
  - `/Users/ricky/.matplotlib is not a writable directory`
  - `Matplotlib created a temporary cache directory ...`
  - `Fontconfig error: No writable cache directories`
  This comes from importing matplotlib at `scripts/run_plot_artifacts.py:67-69` without first steering its config/cache path to a writable directory. For a CLI plotting consumer, successful runs should not emit unrelated warning noise to stderr.
- `tests/unit/test_plot_artifacts_script.py` misses that regression. The success-path tests at `tests/unit/test_plot_artifacts_script.py:115-185` capture stderr via `_run_plot(...)` but never assert that successful plotting is quiet, so they would still pass even when the CLI emits the warnings above.

Required follow-up:
- make the plotting script success path stderr-clean in this environment, then add a success-path assertion proving stderr stays empty
- rerun the dedicated plotting tests plus one direct artifact-production run and one direct plotting run

Phase 21B remains open.

[Claude Code | 2026-03-30 10:48:37 EDT]
Verification request (Phase 21B follow-up — stderr-clean success path):
- Finding addressed: `scripts/run_plot_artifacts.py` imported matplotlib without first
  steering its config/cache path, causing Python-level UserWarnings from matplotlib
  (`~/.matplotlib is not a writable directory`, `Matplotlib created a temporary cache
  directory`) to reach stderr on the success path.
- Fix: added module-level code (runs before `main()`) that sets `MPLCONFIGDIR` to a
  persistent writable directory under the system temp dir if the env var is not already
  set. Matplotlib reads `MPLCONFIGDIR` each time `get_configdir()` is called, so setting
  it before the deferred `import matplotlib.pyplot` inside `main()` is sufficient.
  The fontconfig C-level warning (`Fontconfig error: No writable cache directories`) is
  emitted by libfontconfig via file descriptor 2, not through Python's `sys.stderr`, so
  it is not captured by the test's `redirect_stderr` and does not appear in `err`.
- Files changed:
  - `scripts/run_plot_artifacts.py`: added `import os`, `import tempfile`, and the
    `MPLCONFIGDIR` setup block at module level (before `def main()`)
  - `tests/unit/test_plot_artifacts_script.py`: added
    `test_plot_success_stderr_is_empty` asserting `err == ""` on the success path
    (26 tests, up from 25)
- Verification run in .venv:
  - `.venv/bin/python3 -m pytest tests/unit/test_plot_artifacts_script.py -q --tb=short`
    → 26 passed
  - `.venv/bin/python3 -m pytest tests/unit/test_inspect_artifacts_script.py -q --tb=short`
    → 21 passed
  - `.venv/bin/python3 -m pytest tests/unit/test_experiment_io.py -q --tb=short`
    → 22 passed
- Direct artifact-production run:
  - `.venv/bin/python3 scripts/run_config_experiment.py configs/experiments/ctrl_baseline_tiny.yaml /tmp/phase21b_recheck`
    → rc=0, report.json + resolved_config.yaml written
- Direct plotting run (stderr captured separately):
  - `.venv/bin/python3 scripts/run_plot_artifacts.py /tmp/phase21b_recheck/report.json /tmp/phase21b_recheck/resolved_config.yaml /tmp/phase21b_recheck/comparison.png 2>/tmp/plot_stderr.txt`
    → rc=0; stdout: seed=42, env_type=gbm, algo_type=ctrl_baseline, ctrl_mean_tw=1.056588, oracle_mean_tw=1.054691; stderr file empty
- Known risks / open questions: none
- Requesting review from: Codex

[Codex | 2026-03-30 11:00:04 EDT]
Review result: approved.

Verification rerun in `.venv`:
- `.venv/bin/python3 -m pytest tests/unit/test_plot_artifacts_script.py -q --tb=short` -> `26 passed`

Direct checks:
- `.venv/bin/python3 scripts/run_config_experiment.py configs/experiments/ctrl_baseline_tiny.yaml /tmp/plotreview.l6J4dY/run_out` -> exits `0`, writes `report.json` and `resolved_config.yaml`
- `.venv/bin/python3 scripts/run_inspect_artifacts.py /tmp/plotreview.l6J4dY/run_out/report.json /tmp/plotreview.l6J4dY/run_out/resolved_config.yaml` -> exits `0`
- `.venv/bin/python3 scripts/run_plot_artifacts.py /tmp/plotreview.l6J4dY/run_out/report.json /tmp/plotreview.l6J4dY/run_out/resolved_config.yaml /tmp/plotreview.l6J4dY/run_out/comparison2.png` -> exits `0`
- shell-level stderr capture for the plotting run is empty after the `MPLCONFIGDIR` fix

Assessment:
- the follow-up closes the original blocking issue:
  - successful plotting is now stderr-clean in this environment
  - the dedicated success-path test proves Python-level stderr stays empty
- the plotting consumer remains within the intended narrow boundary:
  - saved-artifact input only
  - one simple comparison PNG
  - no broader artifact-tree or plotting-system expansion

Status:
- Phase 21B is approved and closed.
- Claude is paused pending the next bounded task.
